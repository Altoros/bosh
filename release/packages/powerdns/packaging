set -e

echo "Extracting PowerDNS archive ..."

mkdir -p target

if [ "`uname -m`" == "ppc64le" ]; then
VERSION=3.3-1_ppc64el
else
VERSION=3.3.1-1_amd64
fi

ar p powerdns/pdns-static_$VERSION.deb data.tar.gz | tar zx -C target

cp -a target/usr/sbin/pdns_server ${BOSH_INSTALL_TARGET}
